import * as actions from '../actions/index';

const initialState = {
	selectedMember: null,
	mostCommonWord: null,
	swearCounter: null,
};

export const reducer = (state=initialState, action) => {
	let newState = {};
};